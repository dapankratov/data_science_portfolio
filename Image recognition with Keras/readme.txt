Run the project1. Prepare your environmentThe project uses number of libraries that your system might not have. To install them you  can use the second cell of the python file or follow the python warnings.

!-m pip install pymongo
!pip install pickle-mixin 
!pip install bson
!pip install gridfs-fuse
!pip install Keras
!pip install -U scikit-learn
!pip install opencv-python
!pip install numpy
!-m pip install -U matplotlib
2. When you have your environment ready you can run the program (Jupyter notebook -> Run all cells).3. In addition to the python file there is a pic of a bird, that you can use to get same results as we have in the file.